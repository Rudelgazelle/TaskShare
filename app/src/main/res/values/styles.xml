<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- STYLE FOR NUMBER PICKER -->
    <style name="AppTheme.NumberPicker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textSize">18dp</item>
    </style>

    <!-- STYLE FOR NUMBER PICKER -->
    <style name="AppTheme.SwitchActive" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textSize">@dimen/textSize_Switch</item>
        <item name="android:background">@drawable/ic_launcher_background</item>
    </style>

    <style name="AppTheme.SwitchDeactivated" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textSize">@dimen/textSize_Switch</item>
        <item name="android:background">@drawable/ic_launcher_background</item>
    </style>

    <!-- Additional STYLE FOR NAVIGATION DRAWER ITEMS TO NOT GET CUT OFF -->
    <style name="NavItemTextAppearance">
        <item name="android:ellipsize">middle</item>
    </style>

</resources>
